meta {
  name: Set LLM Configuration (GEMINI / OPENAI)
  type: http
  seq: 2
}

put {
  url: {{baseUrl}}/system-config/llm
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  x-superadmin-key: {{superadminKey}}
}

body:json {
  {
    "provider": "GEMINI"
  }
}
