meta {
  name: Update Order Status
  type: http
  uuid: orders-updateStatus
}

put {
  url: {{baseUrl}}/orders/{{orderId}}/status
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{jwtToken}}
}

body:json {
  {
    "status": "{{orderStatus}}"
  }
}
